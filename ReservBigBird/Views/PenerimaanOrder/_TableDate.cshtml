@model IEnumerable<ReservBigBird.APIModel.TerimaOrder2>

@if (ViewBag.tgl != null)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>@ViewBag.bus</th>
                @for (int i = 0; i < ViewBag.tgl.Count; i++)
                {
                    <th>@ViewBag.tgl[i]</th>
                }
                <th>Stock</th>
            </tr>
        </thead>
        <tbody>

            @foreach(var aa in Model)
            {
                <tr>
                    <td>@aa.poolName</td>

                    @foreach(var bb in aa.listJml)
                    {
                        <td>@bb.jml</td>
                    }

                    <td>@aa.stc</td>
                </tr>
            }


            @*@foreach (var aa in ViewBag.tes1)
                {
                    <tr>
                        <td>@aa.poolName</td>
                        @foreach (var bb in aa.listJml)
                        {
                            <td>@bb.jml</td>
                        }

                        @foreach(var bb in aa.data)
                        {
                            <td>@bb.poolName</td>
                            foreach(var cc in bb.listJml)
                            {
                                <td>@cc.jml</td>
                            }
                        }

                    </tr>
                }*@

        </tbody>
    </table>
}

